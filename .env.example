# Copy this file to ".env" and fill in the real values.
# Do NOT commit a real .env file; add .env to .gitignore.

# Supabase project URL (found in dashboard Settings → API)
SUPABASE_URL=https://wyjgdrmizodsosgnduda.supabase.co

# Use the service role key for server-side operations only
SUPABASE_SERVICE_ROLE_KEY=sb_XXXXXXXXXXXXXXXXXXXXXXXXXX

# Database password from Supabase connection info
DATABASE_PASSWORD=your_postgres_password_here

# Optional: custom port for Express server
# PORT=3000
